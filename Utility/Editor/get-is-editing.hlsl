//Make sure its only included once!
#ifndef ISEDITING_INCLUDED
#define ISEDITING_INCLUDED

int _ISEDITING;

void getIsEditing_float(out int isEditing)
{
    isEditing = _ISEDITING;
}

void getIsEditing_half(out int isEditing)
{
    isEditing = _ISEDITING;
}
#endif
